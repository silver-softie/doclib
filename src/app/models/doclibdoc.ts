export class DocLibDoc {
  reference: string;
  title: string;
  author: string;

  constructor(reference: string, title: string, author: string) {
    this.reference = reference;
    this.title = title;
    this.author = author;
  }
}
